import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InfoEquipComponent } from './info-equip.component';

describe('InfoEquipComponent', () => {
  // let component: InfoEquipComponent;
  // let fixture: ComponentFixture<InfoEquipComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ InfoEquipComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(InfoEquipComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
});

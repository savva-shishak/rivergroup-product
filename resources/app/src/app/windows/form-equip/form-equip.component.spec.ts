import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormEquipComponent } from './form-equip.component';

describe('FormEquipComponent', () => {
  // let component: FormEquipComponent;
  // let fixture: ComponentFixture<FormEquipComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ FormEquipComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(FormEquipComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   //expect(component).toBeTruthy();
  // });
});
